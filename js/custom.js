window.addEventListener('load', (event) => {
    console.log('page is fully loaded');
    let bg = document.getElementById("bg");
    setInterval(changeBg(), 3000);
    changeBg => { for(let i=1;i <= 3;i++){
        
    }};




});